// Code generated by opnsense-cli/generate; DO NOT EDIT.

package api

import (
	"github.com/jontk/opnsense-cli/opnsense"
{% for mod in modules %}
	"github.com/jontk/opnsense-cli/opnsense/{{ mod.package_name }}"
{% endfor %}
)

// API provides access to all OPNsense API modules.
type API struct {
{% for mod in modules %}
	{{ mod.field_name }} *{{ mod.package_name }}.Client
{% endfor %}
}

// NewAPI creates a new API instance with all module clients.
func NewAPI(c *opnsense.Client) *API {
	return &API{
{% for mod in modules %}
		{{ mod.field_name }}: {{ mod.package_name }}.NewClient(c),
{% endfor %}
	}
}
